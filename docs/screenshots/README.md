Screenshots directory for Admin docs.

Suggested filenames (place image files here):
- admin-audit-logs.png — `Audit Logs` open with filters filled and detail modal visible.
- add-guest-modal.png — `Add Guest` modal open with sample values.
- export-csv.png — Filters set and Export CSV UI in view.
- reset-rate-limits.png — Example email shown and result alert visible.

How to add screenshots:
1. Take screenshots locally (start dev server with `npm run dev`).
2. Save images in this folder with the suggested names.
3. Commit them and update `docs/ADMIN.md` if you used different filenames.
