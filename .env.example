# Copy to .env.local and fill values
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Admin password for a simple admin page (not secure for production)
ADMIN_PASSWORD=

# When deploying to Vercel, set the above environment variables in the Project Settings > Environment Variables.